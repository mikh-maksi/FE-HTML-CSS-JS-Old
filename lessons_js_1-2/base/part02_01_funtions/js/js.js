	function square(a,b)
    {
      return a*b;
    };
    function len(x1,y1,x2,y2)
    {
      return Math.sqrt(Math.pow(x1-x2,2)+Math.pow(y1-y2,2));
    };
    function gip (a,b)
    {
      return  Math.sqrt(a*a+b*b);
    }