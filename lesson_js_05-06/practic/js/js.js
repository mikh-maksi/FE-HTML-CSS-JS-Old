console.log("test");
addEventListener("click",calculate);
function calculate(){
	console.log("calculate");
	//объявление переменных и задание тестовых значений
	var send, recive, service_type, send_type, wt, size_w, size_h, size_d,number,cost,pack,back;
	 send=1, recive=1, service_type=1, send_type=1, wt=1, size_w=1, size_h=1, size_d=1,number=1,cost=1,pack=1,back=1;
	//получение значений, вывод их на экран.
	
	//задание формулы.
}

